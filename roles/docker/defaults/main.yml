---
# Docker ensure state: latest or absent
docker_ensure: "latest"

# System user to add to docker group
docker_group: ["docker", "sudo"]
docker_user: docker
docker_rootless: false

# Use apt pinning to install specific versions of Docker packages
use_apt_pin_version: true

docker_networks: []
